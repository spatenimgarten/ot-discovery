# Core dependencies
netifaces>=0.11.0

# Optional: enables a fast Npcap-based ARP sweep on Windows (falls back to the
# built-in SendARP API if not installed, or if no Npcap driver is present)
scapy>=2.5.0

# For development
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0