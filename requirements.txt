# Core dependencies
netifaces>=0.11.0; sys_platform != "win32"
netifaces2>=0.0.22; sys_platform == "win32"

# Enables the fast Npcap-based ARP/DCP sweep on Windows (falls back to the
# built-in SendARP API, and skips DCP, if the Npcap driver is not installed)
scapy>=2.5.0

# For development
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0